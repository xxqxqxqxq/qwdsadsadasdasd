[phases.setup]
nixPkgs = ["python312", "prisma"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate",
  "pip install -r requirements.txt",
  "prisma generate"
]

[phases.build]
cmds = ["prisma generate"]

[deploy]
startCommand = "python main.py"